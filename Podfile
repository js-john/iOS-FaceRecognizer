# Uncomment the next line to define a global platform for your project
platform :ios, '14.0'

target 'Face Recognize' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Face Recognize
  pod 'OpenCV', '~> 3.0.0'
  target 'Face RecognizeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Face RecognizeUITests' do
    # Pods for testing
  end

end
